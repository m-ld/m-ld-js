export const $base = 'http://www.w3.org/ns/shacl#';

export const targetClass = `${$base}targetClass`;
export const path = `${$base}path`;
export const name = `${$base}name`;
export const minCount = `${$base}minCount`;
export const maxCount = `${$base}maxCount`;