/**
 * Internal namespace for node-quadstore based m-ld engine
 */
export const $base = 'http://qs.m-ld.org/';

export const tids = `${$base}tids`; // Transaction IDs graph name
export const tid = `${$base}#tid`; // Property of triple hash
export const thash = `${$base}thash/`; /* Namespace for triple hashes*/ 
